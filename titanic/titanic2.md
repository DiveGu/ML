#### 1 读取数据

- 查看列、df的描述信息

#### 2 观察特征的缺失情况、类型、含义

- 观察特征列的类型：数值型、分类型、文本类型
- 观察缺失值

#### 3 数据可视化

- 看train中各个特征列和y的关系
  - 分类特征、barplot
  - 数值特征、分桶，看barplot 
- 当对train中进行分段操作时，需要也对test进行操作（不如concat，加一个区分列，方便处理数据）
- Q：比如x=age；将x处理成分段，然后画出直方图，比如结果显示y=0.6，那么表示对应的x的y均值是0.6，即60%的x段y=1。*（而不是y=1中每种x的比例）*

- 创造新特征：
  - 将Age离散化，分桶
  - 数据集中Cabin是否为空，新特征CabinBool（发现有记录的存活率>没有记录的存活率）

#### 4 清洗数据

- 查看test的缺失值
- 丢弃不用的列
- 填充缺失值
  - 某些列：最频繁填充
  - AgeGroup列（按照设置的年龄段打上label），来预测这一列的缺失值（'Unknown'）：①先将姓名分组；②按照姓名分组用AgeGroup众数填充AgeGroup的缺失值，**只使用了train的众数**；③将str型的label转化为1、2、3、......
  - **fare列：①按照pclass的平均值来填充；②新建一列，取值为fare的数值型分桶；③删掉fare列** *（Q：代码中直接train中分4桶，test分4桶；不需要concat然后统一分桶吗?）*
- 编码特征col
  - 将分类型的特征col进行编码：不是编码成one-hot，而是1、2、3这种
  - 将离散值分桶，按相同比例划开

#### 5 模型

- 划分train数据，使用0.22的train作为test（val）；0.78的train用来训练
- 构造五个模型，查看准确率：
  - 创造模型对象
  - fit(x_train,y_train)
  - 得test（val）得预测，y_pred；y_pred与y_val计算准确率