#### 1 导入训练集和测试集

- head观察多少列，明确可用的特征列有79列（特征不包含id列）

- concat 训练集和测试集为df，后续预处理要在df基础上进行

#### 2 数据预处理

##### 2.1 计算每一列的缺失比例，丢弃缺失比例大的列

- 计算每一列空值比例 colname-percent
- 转化为pd，排序展现
- 筛选出空值比例较大的列 丢弃

##### 2.2 明确连续性和离散型的列

- 根据列类型将df分成两个df：object_columns_df，numerical_columns_df
- 分类型列
  - 先计算每列空值数量 展示
  - 根据具体含义补全空值，根据实际情况：
    - 将某些列用 None 填空值
    - 某些列用频数最大的取值填空值
- 数值型列
  - 先计算每列空值数量 展示
  - 根据具体含义补全空值，根据实际情况：
    - 某些列用中位数填充
    - 某些列用0填充

##### 2.3 特征选择 删掉一些无用特征

- 获得干净的特征列之后，通过plot观察，删掉一些低方差的列
  - 分类型列画 values_count
  - 数值型画 散点图

##### 2.4 特征工程 创造一些新的特征

- 房子年龄：两特征列相减；构造完之后describe检查有无错误
- total_x：有的同一特征列分成了多个列，totalx=x1+x2+..

##### 2.5 将分类特征列用one-hot表示

- 有序的分类特征 map到0~N
- 无序的分类特征 处理成one-hot

##### 2.6 重新分开训练集和测试集

- concat 分类特征和数值特征
- 去除id列；区分train和test；找出train的target

#### 3 模型

##### 3.1 从df_train中划分 train_train 和 train_test



##### 3.2 用train_train训练模型；用df_train fit模型



##### 3.3 预测test 模型融合

